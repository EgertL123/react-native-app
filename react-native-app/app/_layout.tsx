import React from "react";
import {SafeAreaView} from "react-native";
import Splash from "./(tabs)/index";

const Layout = () => {
    return (
        <SafeAreaView>
            <Splash />
        </SafeAreaView>
    )
}

export default Layout;